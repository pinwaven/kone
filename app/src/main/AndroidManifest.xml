<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 设备管理员权限 -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />

    <!-- 关机/重启 -->
    <uses-permission android:name="android.permission.REBOOT" />
    <uses-permission android:name="android.permission.SHUTDOWN" />

    <!-- 监听开机广播 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 针对 Android 10+ 的后台启动限制 -->
    <uses-permission android:name="android.permission.START_ACTIVITIES_FROM_BACKGROUND" />


    <!-- ✅ 网络权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 下载权限 -->
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <!-- 安装权限 (Android 8.0+) -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- ❌ Android 11+ `WRITE_EXTERNAL_STORAGE` 无效，建议删除 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- ✅ 如果需要访问外部存储，使用 MediaStore API，不建议使用 MANAGE_EXTERNAL_STORAGE -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage"
        tools:remove="android:permission" />

    <!-- ❌ MOUNT_UNMOUNT_FILESYSTEMS 是受保护权限，Android 7+ 已弃用，删除 -->
    <!-- <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"
        tools:ignore="ProtectedPermissions" /> -->

    <!-- ✅ WiFi 相关权限（适配 Android 12+） -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!--    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ✅ Android 12+ WiFi 设备扫描需要 NEARBY_WIFI_DEVICES -->
    <!--    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />-->

    <!-- ✅ 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ✅ USB 设备访问 -->
    <uses-feature android:name="android.hardware.usb.host" />
    <uses-feature android:name="android.hardware.usb.accessory" />

    <!-- ✅ 适配 Android 12+ 蓝牙权限 -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <application
        android:name="poct.device.app.App"
        android:allowBackup="true"
        android:configChanges="orientation|keyboardHidden|screenSize"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WlkAppFullscreen"
        android:usesCleartextTraffic="false">
        <!-- ❌ Android 11+ `requestLegacyExternalStorage` 无效，建议删除 -->

        <activity
            android:name="poct.device.app.MainActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:lockTaskMode="if_whitelisted"
            android:theme="@style/Theme.WlkFullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name="poct.device.app.MyDeviceAdminReceiver"
            android:description="@string/device_admin_description"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="poct.device.app.BootCompleteReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <!-- 标准开机广播 -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- 兼容部分厂商（如华为/小米） -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- 锁定屏幕完成事件（可选） -->
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 添加 FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/device_upgrade_config" />
        </provider>
    </application>
</manifest>

